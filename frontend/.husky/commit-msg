#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run commitlint to validate commit message format
npx --no -- commitlint --edit $1

# Check for ClarityOS™ compliance notes in commit message
if grep -q "ClarityOS\|clarity" "$1"; then
  echo "✅ ClarityOS™ compliance noted in commit message"
else
  echo "📋 Consider adding ClarityOS™ compliance notes for UI changes"
fi 